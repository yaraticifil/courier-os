package com.example.androidprototype.ui.theme

import androidx.compose.ui.graphics.Color

// Core Dark Theme Palette
val BackgroundDark = Color(0xFF060608)      // Sleek deep dark (#060608)
val SurfaceDark = Color(0xFF0D0D11)         // Card surface dark (#0D0D11)
val SurfaceVariantDark = Color(0xFF16161C)  // Lighter surface dark (#16161C)

val PrimaryPurple = Color(0xFF7F77DD)       // Vibrant purple accent (#7F77DD)
val PrimaryGreen = Color(0xFF1D9E75)        // Vibrant green accent (#1D9E75)

val TextPrimary = Color(0xFFF4F4F5)         // Bright soft white (#f4f4f5)
val TextSecondary = Color(0xFFA1A1AA)       // Muted gray (#a1a1aa)
val TextTertiary = Color(0xFF52525B)        // Dark gray-blue (#52525b)

// Glassmorphism effect colors
val GlassBorder = Color(0x1AFFFFFF)          // Transparent white border
val GlassBg = Color(0x14FFFFFF)              // Light glass overlay (slightly more opaque for better legibility)

// Custom Gamified Gradients
val ProfileRingGradient = listOf(
    Color(0xFF7F77DD),
    Color(0xFF1D9E75),
    Color(0xFF7F77DD)
)

val StatCardGradient = listOf(
    Color(0xFF1B1B2A),
    Color(0xFF13131F)
)

val ProgressTrackColor = Color(0xFF1F1F30)
